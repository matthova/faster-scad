---
"quito-release-root": minor
---

web: the marketing page is now the site root (/faster-scad/) and the playground moved to /faster-scad/playground (breaking: bookmarks to the old editor root now land on the marketing page; use /playground)
