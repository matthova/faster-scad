---
"quito-release-root": patch
---

web: the editor's desktop-app callout now downloads the right build for your OS directly (macOS Apple Silicon/Intel, Windows, Linux) instead of routing through /about, and is hidden on phones/tablets where there's no desktop build; the "Quito playground" wordmark now links to /about
