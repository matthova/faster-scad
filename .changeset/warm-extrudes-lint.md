---
"quito-release-root": patch
---

warn on dead (overwritten) assignments in your own source, add `rotate_extrude(start=)` for partial sweeps, and lock `linear_extrude(v=)` oblique extrudes to the geometry oracle
