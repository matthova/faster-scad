---
"openrscad-release-root": patch
---

serialize function values to OpenSCAD's `function(params) body` text (str/echo), fix `is_num(nan)` and exact `tan()` at 45° multiples, and honor BOSL2 `expect_success` error tests — BOSL2 function-suite coverage rises from 428/513 to 503/513
