---
"quito-release-root": minor
---

Web UI (M9): add a **theme toggle** — Auto / Light / Dark. The playground previously always followed the OS appearance with no way to override it; now the choice persists across reloads. Available from the command palette (⌘K → "Theme: …").
