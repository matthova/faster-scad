/target
**/*.rs.bk
Cargo.lock.orig
*.stl
!benches/**/*.stl
!corpus/geom/*.stl
.DS_Store

# node / web (later milestones)
node_modules/
dist/
# wasm-pack build output (regenerate with `npm run build:wasm`)
web/engine/
# quito-engine npm package build output (regenerate with `npm run build` in packages/npm)
packages/npm/pkg/
packages/npm/*.tgz

# TypeScript incremental build info
*.tsbuildinfo
