{
  "name": "quito-desktop",
  "version": "0.2.0",
  "private": true,
  "description": "Quito desktop app (Tauri v2) — native OpenSCAD-compatible renderer.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev --config src-tauri/tauri.conf.json",
    "build": "tauri build --config src-tauri/tauri.conf.json"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
